# Deferred Work

## Deferred from: code review of 5-2-auto-accept-single-invitation-with-toast-decline (2026-06-20)

- **invite-verify full token redesign (residual of applied D1 patch)** [`InviteLinkVerificationResource.java` / `TenantInvitationEntity.java`] — the code-review patch closed the exploitable part (raw token no longer logged/span-tagged; the id is a random UUID so enumeration is infeasible). The remaining hardening — a signed/expiring action token consumed on first use, instead of the bare invitation id as a permanent unauthenticated verifier — requires a schema change (add a nonce + issued/expiry timestamp to `TenantInvitationEntity`, update `EmailSender`/email FTL to embed the signed token, consume on verify). Belongs to a dedicated 5-1 security-hardening change with a DB migration.
- **Working tree bundles four stories' changes together** — story 5-1 (invite-verify resource, `EmailSender`, invitation-email FTL, `TenantInvitationsResource`, `TenantProvider`/`JpaTenantProvider`/`TenantInvitationEntity.findInvitationById`), 3-3/3-4 (`PromptPasskeyEnrollment` + SPI registration), and 4-1/4-2 (`TenantsBean` lastUsed/sorting, `SelectActiveTenant` zero-account routing + `ACTIVE_TENANT_ATTRIBUTE`) are all uncommitted alongside 5-2. The `TenantsBean` change also violates 5-2 Dev Note 9 ("Do NOT modify TenantsBean") and silently changes the logoUrl fallback from a CDN URL to `""`. Recommend committing per-story so each is reviewed under its own AC.
- **Synchronous UserService call on the login thread** [`ReviewTenantInvitations.java:128`] — up to ~25s connect+read timeout blocks the login request; "non-blocking" only covers exceptions, not latency. Owned by story 5-4 (async refactor / retry banner).
- **email-not-verified `else` branch never completes the required action** [`ReviewTenantInvitations.java:104`] — logs a warning but calls no `success()/challenge()/failure()`, so the auth flow can stall for an invitee whose email is unverified. Pre-existing structure, not introduced by 5-2.
- **Session-note propagation on SSO re-auth** [`ReviewTenantInvitations.java:167` → `ToastInfoResource`] — `setUserSessionNote`→`getNote` may not refresh notes when a user session already exists (SSO re-auth), so the toast can be missed. Narrow edge; the base pattern matches the working `ActiveTenantMapper`.
- **invite-verify redirect/lookup inconsistencies** [`InviteLinkVerificationResource.java`] — redirects to `accountBase` (rest of feature uses `realmLoginPage`); `getUserByEmail` is ambiguous when the realm has `duplicateEmailsAllowed`. Story 5-1 scope.
- **Decline logout guarantee untested** [`InviteDeclineTest.java`] — tests assert membership revoked but never assert the browser session cookie is actually invalidated (AC-3 "logged out of all sessions").
- **One-time toast can be consumed by the wrong reader** [`ToastInfoResource.java:840`] — server clears notes on first read, so a double-fetch (React strict mode) or a second tab can eat the toast before the intended tab renders it. Acceptable per current design.

## Deferred from: code review of 4-4-skeleton-loading-state (2026-06-17)

- **`aria-busy="true"` on `#account-list` never clears when JS is disabled** [`azguards-keycloak-custom-theme/.../login/select-tenant.ftl`] — Story 4-4 introduced `aria-busy="true"` + 3 `.wt-skeleton-row` placeholders, removed only by the inline JS teardown. With JS disabled the real tenant buttons are still present and clickable, but `aria-busy="true"` persists so assistive tech announces the list as perpetually "Loading accounts…", and the skeleton rows (though `aria-hidden`) remain painted. Same family as the already-deferred "No `<noscript>` fallback for the search field" item below (4-3). A `<noscript>` CSS rule can hide `.wt-skeleton-row` visually, but `aria-busy` cannot be cleared without JS — revisit in a theme-wide noscript/graceful-degradation hardening pass. Low: JS is effectively required for the broader login flow (passkey, etc.).

## Deferred from: code review of 4-3-conditional-search-no-match-state (2026-06-17)

- **Unguarded `kcSanitize(tenant.name)` in `aria-label`/name span** [`azguards-keycloak-custom-theme/.../login/select-tenant.ftl:~70,~86`] — `data-name` and the initials block defensively use `(tenant.name!"")`, but `aria-label` and the name `<span>` pass bare `kcSanitize(tenant.name)`. NOT reachable today (`TenantEntity.NAME` is `@Column(nullable = false)`); defensive consistency only — `kcSanitize(tenant.name!"")`. Same field as the 4.2 comparator null-safety note.
- **Broader attribute-context escaping pass** [`select-tenant.ftl` — `data-name`, button `aria-label`, `value="${tenant.id}"`] — none are `?html`-escaped; a `"` in a tenant name would break the attribute / a row's search token. Spec 4.3 (lines 140/401) explicitly acknowledges and defers this; same family as the existing 4.2 attribute-escaping note. IDs are UUIDs (no current risk).
- **`kcSanitize(name)` vs `?lower_case` data-name divergence** [`select-tenant.ftl`] — displayed name (sanitized) and the searchable `data-name` (raw, lowercased) use different transforms, so a name containing markup kcSanitize strips could be visible but unsearchable by the prefix the user sees. Edge case.
- **No-match `aria-live="polite"` may not announce when toggled from `hidden`** [`select-tenant.ftl`] — some screen readers drop announcements for a live region that enters the a11y tree at the same tick its text is set. a11y enhancement (e.g. keep node present, toggle a visibility class).
- **No `<noscript>` fallback for the search field** [`select-tenant.ftl`] — with JS disabled the search input renders but does nothing (account rows remain clickable via native submit). Graceful-degradation gap; minor.
- **`String.replace('{query}', …)` substitutes only the first occurrence** [`select-tenant.ftl`] — only matters if a future/localized `selectAccountNoMatch` contains `{query}` more than once. Use a global regex if that ever happens.

## Deferred from: code review of story-4.2 Repo 2 / theme (2026-06-17)

- **Attribute-context escaping not guaranteed for `"`** [`azguards-keycloak-custom-theme/.../login/select-tenant.ftl` — button `aria-label`, `<img src>`] — `kcSanitize(tenant.name)` inside `aria-label="..."` and raw `${tenant.logoUrl}` inside `src="..."` are HTML-body sanitizers and don't guarantee double-quote encoding for attribute context (breakout risk). Pre-existing project-wide convention (the prior template did the same); revisit in a theme-wide attribute-escaping hardening pass.
- **`review-invitations.ftl` references a missing `default-logo.png`** [`azguards-keycloak-custom-theme/.../login/review-invitations.ftl`] — the asset does not exist under `resources/img/`; the `onerror` fallback is dead. Pre-existing and out of Story 4-2 scope (this story removed the analogous dead reference from `select-tenant.ftl`). Fix on the invitations screen separately (drop the fallback or restore an asset / use initials).
- **Hardcoded `" — Last used"` punctuation in `aria-label`** [`select-tenant.ftl`] — em-dash + label concatenated as a literal; non-localized and assumes LTR. Minor screen-reader/i18n polish.
- **`role="main"` div + `<h1 tabindex="-1">` autofocus may double-announce** [`select-tenant.ftl`] — consistent with the project-wide focus-management pattern; minor a11y, revisit if SR double-announcement is reported.

## Deferred from: code review of story-4.2 (2026-06-17)

- **Zero-accounts routing change is out of scope for Story 4-2** [`SelectActiveTenant.java:70-74`] — the `tenantMemberships.isEmpty()` branch adds `routing.zero_accounts` tracing + `context.getUser().addRequiredAction(CreateTenant.ID)`. Story 4-2 Task 3 only covers the multi-tenant `lastUsed` read and the `processAction` persist; this is functional routing belonging to Story 4-1, bundled into the shared working tree. Of questionable reachability (`evaluateTriggers` only queues the action when `size > 1`; IDP path throws `ACCESS_DENIED` on empty). **Decision 2026-06-17:** belongs to Story 4-1 (conditional account routing); re-attributed there, not reverted for 4-2.
- **`active_tenant` persisted only on manual pick, never on single-tenant auto-select** [`SelectActiveTenant.java:77,115`] — `processAction:115` writes the attribute, but the `size() == 1` auto-select branch (`:77`) only sets the session note, so a user who auto-selects a lone tenant then later joins a second sees the picker with no "Last used" pin (AC #2 transition gap). **Decision 2026-06-17:** minor cross-session transition edge; single-tenant users don't see the picker — revisit if it surfaces.
- **Comparator/`equals` lacks null-safety on tenant name** [`TenantsBean.java:49`] — `Comparator.comparing(Tenant::isLastUsed).reversed().thenComparing(Tenant::getName)` would NPE if a tenant name were null. Not reachable today: `TenantEntity.NAME` is `@Column(nullable = false)`. Optional defensive hardening (`Comparator.nullsLast(Comparator.naturalOrder())`) only — no current risk.
- **`SwitchActiveTenant` still uses the hardcoded `"active_tenant"` literal** [`SwitchActiveTenant.java:~93`] — the new `Constants.ACTIVE_TENANT_ATTRIBUTE` is defined and consumed by `SelectActiveTenant`, but `SwitchActiveTenant` retains a private literal. Story 4.2 explicitly forbids refactoring `SwitchActiveTenant`; consolidate to the constant in a future cleanup PR to prevent literal drift.
- **Stale `active_tenant` attribute never reconciled on membership loss** [`SelectActiveTenant.java` / `TenantsBean.java`] — when a user loses membership in their previously-selected tenant, the `active_tenant` user attribute is never cleared. Harmless for the picker (a stale id simply matches no row, so nothing is pinned), but the attribute lingers; revisit if any future logic trusts it for default selection.

## Deferred from: code review of 4-1-conditional-account-selection-routing (2026-06-17)

- **No `active-tenant-id` session-note short-circuit in `requiredActionChallenge`** [`SelectActiveTenant.java:69-74`] — unlike `evaluateTriggers` (line 40), the challenge path never checks `ACTIVE_TENANT_ID_SESSION_NOTE` before deciding, so a user who already has an active-tenant note but currently shows zero filtered memberships is re-routed into create-tenant. Pre-existing and already acknowledged in the Story 4.1 spec (§"Deferred Items") and the 2-4 review entry; the new zero-account branch improves on the prior dead-end pass-through rather than worsening it. Low-risk; revisit alongside the `requiredActionChallenge` logic-drift cleanup.
- **Filtered vs unfiltered membership view differs between `SelectActiveTenant` and `CreateTenant`** [`SelectActiveTenant.java:154-171` vs `CreateTenant.java:35`] — `SelectActiveTenant` decides on the IDP-*filtered* list while `CreateTenant.evaluateTriggers` uses the *unfiltered* `getTenantMembershipsStream`. For IDP-tenant-specific sessions with zero accessible tenants, `getFilteredTenantMemberships` throws `ACCESS_DENIED` at line 164 *before* the new zero-account branch is reached, so the new routing only ever fires for non-IDP / non-tenant-specific sessions (where filtered == unfiltered). Not introduced by this change; the throw at :164 prevents the hypothesized create↔select loop. Revisit if IDP-scoped users should be routed to create-tenant instead of getting ACCESS_DENIED.

## Deferred from: code review of story-3.4 (2026-06-17)

- **Standalone FTL has no session-expired scaffolding** [`azguards-keycloak-custom-theme/.../login/passkey-enrollment-prompt.ftl:40-49`] — the spec mandates standalone HTML (no `@layout.registrationLayout`, constraint #3) and this is consistent across all passkey FTLs (`webauthn-register.ftl`, `webauthn-authenticate.ftl`). Consequence: no built-in error/expiry page handling; if `${url.loginAction}` is ever unresolved (template rendered outside a challenge context, or session expired between challenge and submit), the POST targets an indeterminate endpoint and the user sees a generic error rather than a session-expired page. Epic-wide consequence of the mandated standalone-FTL pattern, not specific to Story 3.4. Revisit during a passkey-FTL hardening pass.
- **Auto-dismiss has no bfcache/`pageshow` guard** [`passkey-enrollment-prompt.ftl:62-67`] — the AC #5 unsupported-browser auto-submit (`dismissForm.submit()`) has no `pageshow`/`event.persisted` guard, so a back-navigation/bfcache restore can re-fire a stale dismiss POST against an already-completed/expired execution, surfacing an error page. Minor; shared by all auto-submitting forms across the epic. Core AC #5 behavior is correct.
- **`catch (Exception)` lets `Error` mislabel the tracing span as successful** [`PromptPasskeyEnrollment.java` — all 3 methods] — the canonical tracing pattern catches `Exception` (not `Throwable`), so an `Error` escaping the body skips `traceError = ex` and `finally` calls `finishSpan(span, null)`, recording a failed call as a successful span. Spec-dictated pattern shared by every instrumented class; folds into the existing systemic "harden `TracingHelper.startServerSpan` to be fail-safe" item (Story 1.6 review, 2026-06-13 2nd pass).

## Deferred from: code review of story-3.2 (2026-06-15)

- **Empty `webAuthnPolicyRpId` may break passkey validation across tenant hostnames** [`src/test/resources/realm-export.json:392`] — `webAuthnPolicyRpId` is `""`, so Keycloak defaults the RP ID to the request host. In a multi-tenant deployment served under multiple hostnames, a passkey registered while on one host's effective RP ID will fail the assertion (`navigator.credentials.get`) when the user later authenticates from a different hostname. Pre-existing realm config — not introduced by Story 3.2's flow-wiring/i18n change — but directly affects whether passkey-first auth works across tenants. Revisit when the deployment hostname strategy is finalized (set RP ID to the registrable parent domain).

## Deferred from: code review of story-3.1 (2026-06-15)

- **Dirty working tree — unrelated Story 2.4 / tenant-CRUD changes pollute the story diff** — Repo 1 (`keycloak-multi-tenancy`) carries many uncommitted, unrelated modified files (Story 2.4 magic-link + tenant-CRUD Java/tests) that were not stashed/committed before Story 3.1 started, contradicting the Epic 2 retro S1 lesson. Story 3.1's own touched files are correct, but the unrelated changes should be separated/committed before merge so the story diff is clean.
- **External logo dependency with no SRI in a security-critical auth flow** [`azguards-keycloak-custom-theme/.../login/webauthn-register.ftl:19-23`] — the brand mark loads from `https://www.whatatalk.com/...` with a local `onerror` fallback. Copied verbatim from the existing `login.ftl` pattern (consistent project-wide), so deferred as a pre-existing convention; revisit if self-hosting the primary logo / adding SRI becomes a broader hardening task.
- **Unsupported-browser / insecure-context dead-end** [`azguards-keycloak-custom-theme/.../login/webauthn-register.ftl:134, 152-153`] — no `window.PublicKeyCredential` feature-detect; on an unsupported browser or non-secure context `navigator.credentials.create` throws a synchronous `TypeError` outside the promise chain, leaving the primary button disabled and the status region stuck on "in progress…" with no fallback. Deferred by decision (2026-06-15): Story 3.3 (graceful passkey fallback / degradation) owns the comprehensive fix.

## Deferred from: code review of story-2.4 (2026-06-15, magic-link Repo 1 scope)

- **Magic-link email body text is hardcoded English** [`templates/html/magic-link-email.ftl`, `templates/text/magic-link-email.ftl`] — the email subject (`magicLinkEmailSubject`) and all UI strings are externalized in `messages_en.properties`, but the email body copy ("Use the link below to sign in…", brand wording) is hardcoded English in the templates. Inconsistent localization. Deferred to a broader i18n pass.
- **Resend cooldown is per-auth-session only → no cross-session throttle** [`MagicLinkAuthenticator.java:108-122`] — re-confirmed this round. `MAGIC_LINK_LAST_SENT_TS` lives in the auth session; a fresh login flow resets the 60s cooldown, so email-bombing a known verified address is not prevented. Deferred: spec Dev Notes mark cross-session throttling explicitly out of scope; proper fix needs a SingleUseObjectProvider / brute-force store keyed by user/email. (Duplicate of the 2026-06-14 entry; retained for traceability.)

## Deferred from: code review of story-2.4 (2026-06-14)

- **Rate-limit is per-auth-session only → email-bombing bypass** [`MagicLinkAuthenticator.java:88-101`] — the 60s resend cooldown is stored as an auth-session note; starting a fresh login flow resets it, allowing unbounded magic-link emails to a known verified address. No per-user/IP server-side throttle. Deferred: spec Dev Notes mark cross-session throttling explicitly out of scope; proper fix needs a SingleUseObjectProvider / brute-force store keyed by user/email.
- **Token serialization / Urls.actionTokenBuilder failure → generic 500** [`MagicLinkAuthenticator.java:123-131`] — if `token.serialize(...)` or the URL builder throws, the exception propagates to the AuthenticationProcessor as an internal server error instead of a handled "please try again" form. Minor; cooldown timestamp isn't set yet so retry isn't blocked.
- **Feature non-functional until Repo 2 theme + admin flow-wiring** [cross-repo] — AC#2/#6/#7 depend on `login.ftl` method-row + `login-magic-link.ftl` (azguards-keycloak-custom-theme, Repo 2), plus a manual admin step to add the `magic-link` execution to the browser flow as ALTERNATIVE and set the realm-specific `magicLinkAuthExecId` theme property. Repo 1 alone cannot activate the feature. Deployment/cross-repo dependency.

## Deferred from: code review of story-2.3 (2026-06-14)

- **No safety net if the SSO navigation stalls** [`login-with-sso.ftl:96-105`] — on a valid submit the form is hidden and the submit button disabled; if `ssoForm.submit()` never completes (network stall) and the return is not a bfcache restore, the user is left with a hidden form and no recovery control. Minor edge robustness — primary outcomes (IdP redirect / server error re-render) reset the DOM, so deferred.

## Deferred from: code review of story-2.2 (2026-06-14)

- **Decorative ⚠ icon exposed to assistive tech** [`style.css:127`] — `speak: never` is an obsolete no-op; the `.wt-login-error::before` glyph may be announced by screen readers. Needs a real element with `aria-hidden` to reliably silence. Spec-prescribed CSS.
- **Lockout message double-announced** [`login.ftl:104,189`] — `role="alert"` auto-announces on DOM insert and the focus IIFE also `.focus()`es the element, causing screen readers to read the lockout banner twice. Minor a11y.
- **`info`/`success` message types not rendered** [`login.ftl:29`] — `showError` only handles error/warning; info/success messages (logout confirmations, password-reset success) never display on the login page. Pre-existing (old template only showed `error`), out of story 2.2 scope.
- **No fallback focus when `wtA11y` is undefined** [`login.ftl:192`] — if `script.js` fails to load, the credentials-error branch moves no focus (only the locked branch is self-contained). Minor graceful-degradation gap.
- **Inconsistent error-summary contract** [`login.ftl` + `script.js:332`] — `focusFirstError` targets `.wt-form__error-summary` but login uses `.wt-login-error`; works via the `[aria-invalid]` fallback for credentials errors but is a maintainer trap. Naming inconsistency only.
- **bfcache restore doesn't re-run error focus** [`login.ftl:185`] — the parse-time focus IIFE doesn't re-fire on `pageshow`/back-navigation, so a restored locked page won't re-focus the banner. Minor edge case.

## Deferred from: code review of story-1.2 (2026-06-12)

- **Tokenization flattened decorative backgrounds + removed the mobile brand gradient** [`selectTenant.css:250`, `style.css:77`, `reviewTenant.css`] — the two-stop teal→green mobile header gradient is now flat `--wt-bg`, and overlay/band/card backgrounds all collapsed to the same `--wt-bg`, flattening layered depth. **Reason for deferral: needs surface-tint tokens** — restoring depth requires new surface-tint tokens not yet in the design system; belongs with Story 1.3 component styles.
- **Focus ring hard-codes `rgba(15,118,110,.25)` instead of `--wt-focus-ring`** [`style.css:814`] — dark-mode focus ring stays teal instead of the intended brighter `#2DD4BF`; `--wt-focus-ring` goes unused. Focus-style consumption is Story 1.3+ scope; AC#3 only bans hex literals (rgba passes). Needs a color-mix or rgba channel token.
- **Card/overlay `box-shadow` values use literal `rgba(0,0,0,…)` not `--wt-shadow-card`** — shadows are nearly invisible in dark mode; the dark `--wt-shadow-card` variant is defined but unused. Shadow consumption is not in this token-definition story's scope.
- **`font-family: var(--wt-font-family)` has no fallback** — if `tokens.css` fails to load, text falls back to browser-default serif (previously `Arial`). Low risk (tokens.css now linked on every page); add `, sans-serif` fallback opportunistically.
- **No `[data-theme="light"]` escape hatch** — a user on OS dark mode cannot be forced back to light via `data-theme`, since only `@media` + `[data-theme="dark"]` blocks exist. No AC requires forced-light override; future theming-toggle enhancement.

## Deferred from: code review of story-1.3 (2026-06-12)

- **Jenkinsfile: CodeArtifact auth token in workspace `settings.xml`** [`Jenkinsfile`] — token written plaintext, removed only in `post.always`; exposed for the full `mvn deploy` and leaks on hard-abort or workspace archival. Out of CSS-story scope — handle in a separate CI/security PR.
- **Jenkinsfile: AWS CLI `${HOME}/.local` PATH prepend vs conditional install** [`Jenkinsfile`] — non-deterministic across cold vs cached/containerized agents; `aws --version` can fail when HOME differs between Groovy and shell contexts. Out of scope.
- **Bootstrap utility classes not token-aware in dark mode** (`bg-light`, `alert-*`, `text-muted`, `btn-outline-secondary`) — wrong colors under `prefers-color-scheme: dark`. Bootstrap cleanup is Story 1.6 scope.
- **Password-strength label uses Bootstrap `text-success`/`text-danger`** [`login-update-password.ftl`] — not token-aware; diverges from palette in dark mode. Update-password polish.
- **`--wt-info` equals `--wt-primary` (#0F766E)** [`tokens.css`] — strength "strong" indicator is visually indistinct from the brand primary; the semantic weak→fair→good→strong ladder is degraded. Token-palette review.
- **`updatePassword.css` generic class names** (`.requirement`, `.strength-fill`, `.validation-error`) vs `.wt-` convention — cross-page style-bleed risk if loaded globally. Namespacing follow-up.
- **`--wt-danger-border` used inconsistently** [`components.css`/`updatePassword.css`] — ignored by `.wt-field--error` (uses `--wt-danger`), used by `.validation-error`; token is partly dead. Token-consistency cleanup.
- **`.wt-field__error::before { content: '⚠' }`** [`components.css`] — pseudo-element glyph as the sole error affordance: renders as color emoji on some platforms, tofu on minimal font stacks, and is unreliably exposed to assistive tech. Component polish (class not yet used by templates).
- **login-reset-password.ftl drops font-awesome** [`login-reset-password.ftl`] — sibling password screens keep an icon font; inconsistent icon availability across the three password screens.
- **`.wt-btn--submitting` mixes `display:block` + `inline-flex` with `width:100%`** [`components.css`] — inline-level box forced full width; behaves inconsistently across layout contexts. Prefer `display:flex`. Low risk.
- **Literal `rgba(0,0,0,…)` box-shadows remain** in `reviewTenant.css`/`selectTenant.css` — against Task-6 zero-hardcoded-color intent; mostly pre-1.3 work and structural shadows. Revisit during a tokenization sweep.

## Deferred from: code review of 1-5-accessibility-utilities-focus-management.md (2026-06-12)

- **Missing `'use strict'` on `wtToastInit` IIFE** [`script.js:281`] — deferred, pre-existing from Story 1.4.
- **AC #5 axe-core scan not executed** — spec-acknowledged limitation; no automated test infrastructure yet (Story 1.6 scope).

## Deferred from: code review of 1-6-production-hygiene-observability-test-harness-baseline.md (2026-06-13)

- **`failureChallenge` then fall-through to `doAuthenticate`→`success` on disabled IdP** [`IdpTenantMembershipsCreatingAuthenticator.java:44-47`] — missing `return`/`else` on the disabled-IdP path: the flow both fails-challenges and succeeds on the same context (undefined auth outcome). Pre-existing, preserved verbatim through the tracing refactor.
- **User-service status update failure swallowed, then local membership state mutated anyway** [`ReviewTenantInvitations.java:165-171`] — if the user-service REST call throws, the exception is only logged and execution continues to grant memberships / set ACTIVE / revoke invitations locally. Keycloak and the user-service diverge with no compensation. Pre-existing structure.
- **`performLogin` dereferences `getProviderFactory(...)` result without null check** [`LoginWithSsoAuthenticator.java`] — NPE if the provider id is not registered. Pre-existing.
- **In-memory filtering after full-table fetch in `getTenantsStream`** [`JpaTenantProvider.java`] — `nameOrIdQuery`/attribute predicates applied as Java stream filters after materializing the whole tenant table; `first`/`max` pagination then runs over the full result set (no SQL pushdown). Scalability concern; tied to the scope-creep feature decision — needs predicate pushdown rework.

## Deferred from: code review of 1-6-production-hygiene-observability-test-harness-baseline.md (2026-06-13, 2nd pass)

- **Span creation outside try/catch in all instrumented methods** [all 5 tracing classes] — `Span span = TracingHelper.startServerSpan(...)` is the first statement, outside the try block; if tracing-backend init throws it propagates into the login/required-action flow. Spec-dictated canonical pattern (matches `TenantResource`/`SwitchActiveTenant`). Proper fix is to harden `TracingHelper.startServerSpan` to be fail-safe (return a no-op span on backend failure) — systemic, not specific to this story.
- **`PageResolver.resolve` non-waiting visibility checks** [`PageResolver.java`] — waits only for an `h1`, then runs instantaneous `.first().isVisible()` per branch; under CI load can fall through to "Unexpected page" before the target heading paints. Flake risk; 39/39 pass locally.
- **`SelectTenantPage.signIn()` vestigial no-op + brittle `availableOptions()` selector** [`SelectTenantPage.java:14-28`] — `select()` now performs the actual submit via the per-card "Select" button; `signIn()` only re-resolves the page (name no longer matches behavior). `availableOptions()` reads a presentation-coupled `.tenant-info p strong` path. Test-only clarity cleanup.
- **`ReviewInvitationsPage` multi-accept has no inter-click wait** [`ReviewInvitationsPage.java`] — `acceptInvitation/rejectInvitation` click per-card with no wait for card state between clicks; chained multi-accept assumes the card list stays stable across re-render. Flake risk.
- **`doAuthenticate` dereferences `getIdpConfig()` unguarded** [`IdpTenantMembershipsCreatingAuthenticator.java:113-121`] — span-tag block null-guards `brokerContext.getIdpConfig()`, then the body dereferences it unguarded (NPE shape if config is null). Pre-existing; the new guard signals a nullability the body ignores.

## Deferred from: code review of story 2-1-refactored-login-screen (2026-06-13)

- **Jenkinsfile: unpinned AWS CLI download, no checksum/signature verification, `curl` without `-f`** [Jenkinsfile `Setup AWS CLI` stage] — `curl -s https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip | unzip | install` runs unpinned, unverified code in a CI runner holding AWS keys + CodeArtifact token. Use `curl -fsSL`, pin a version, verify the AWS GPG signature. CI hardening, out of this UI story's scope.
- **Bootstrap + Font Awesome CDN still loaded on reset/update/select/review templates** [login-reset-password.ftl, login-update-password.ftl, select-tenant.ftl, review-invitations.ftl] — these retain `cdn.jsdelivr.net` / `cdnjs.cloudflare.com` links alongside the new token CSS. Offline/CDN-down breaks their icons & layout, and only login.ftl was fully migrated. Full migration deferred to Epics 4/5.
- **Global `prefers-reduced-motion` universal `!important` reset** [style.css] — `*, *::before, *::after { animation-duration:.01ms !important; transition-duration:.01ms !important; }` applies site-wide to every themed page, neutralizing intended spinners/transitions (e.g. `.wt-btn__spinner`, tenant-card hover, error-container reveal). Accepted a11y pattern but broad; revisit scoping.
- **`togglePassword` inline onclick lacks null guards** [login-update-password.ftl] — the inline `onclick="togglePassword(...)"` path is independent of the DOMContentLoaded bail-out added in this diff; if markup renders the button without the matching input/icon ids it throws. Pre-existing, low risk.

## Deferred from: code review of 2-4-conditional-email-me-a-sign-in-link-magic-link (2026-06-15)

- **`getTenantsStream` full-table fetch + in-memory filtering** [JpaTenantProvider.java:114-134] — name/attribute filtering happens in application memory with no DB predicate or pagination pushdown; unbounded on large realms. Performance hardening.
- **Pagination bounds not validated** [TenantsResource.java:121-123] — `first`/`max` silently ignore negative/zero and have no upper cap; `max=0` returns the full list. Input-validation hardening.
- **Magic-link poll has no expiry signal** [MagicLinkAuthenticator.java:90-96] — `check` only ever returns `{"verified":false}`; no branch detects a now-expired token to prompt a fresh send. Depends on fixing the CRITICAL token-expiry bug first; resend button mitigates.
- **`SelectActiveTenant.requiredActionChallenge` logic drift** [SelectActiveTenant.java] — re-fetches and re-sets the active-tenant session note without the existing-note short-circuit that `evaluateTriggers` uses. Low-risk cleanup.
- **`magicLinkExpired` dead message key** [messages_en.properties] — defined but never rendered (expired UX uses KC `expiredActionToken*` overrides). Corroborates the known AC-4 resend-path gap.
- **TAW label wording** [Repo 2 login-magic-link.ftl] — always "Use password instead", including the AC-1 `notVerified` state where AC-1 specifies "Use another method". Copy tweak.

## Deferred from: code review of story-3.1 (2026-06-15)

- **No WebAuthn feature detection → silent dead-end** — `webauthn-register.ftl:146` — `navigator.credentials.create` throws a synchronous `TypeError` (bypasses `.catch`) on browsers without WebAuthn or in an insecure (plain-HTTP) context; the register button stays disabled and the status region stuck with no recovery. Deferred to Story 3.3 (graceful passkey fallback/degradation). Re-confirms the prior 2026-06-15 deferral.
- **Unhandled WebAuthn rejection types surfaced as opaque strings** — `webauthn-register.ftl:162-168` — only `NotAllowedError`/`AbortError` are handled; `InvalidStateError` (passkey already registered on this authenticator → confusing, can produce an infinite retry loop), `SecurityError` (rpId/origin mismatch), and `NotSupportedError` fall through to a generic retry with a raw `err.toString()`. Map known error names to friendly messages. Deferred to Story 3.3 graceful degradation.
- **`NotAllowedError` cancel→skip routing also catches timeouts** — `webauthn-register.ftl:162-167` — Patch 6 routes any `NotAllowedError`/`AbortError` to the silent `cancel-aia` skip; WebAuthn also throws `NotAllowedError` on ceremony timeout/platform failure, so a timed-out user is silently treated as "Not now" instead of offered retry. Spec makes cancel vs timeout indistinguishable client-side → no reliable split. Deferred to Story 3.3 graceful degradation. (Decision 2026-06-15)

## Deferred from: code review of story-3.1 (2026-06-15)

- **No WebAuthn feature/secure-context detection** (`webauthn-register.ftl:165`) — on a browser without WebAuthn or in an insecure (plain-HTTP) context, `navigator.credentials` is undefined and `.create` throws synchronously (bypassing `.catch`), leaving the button disabled and the status region stuck with no error. Owned by Story 3.3 (graceful passkey fallback/degradation).
- **Malformed server-rendered base64url → infinite retry loop in non-AIA flow** (`webauthn-register.ftl:153-157`) — corrupt `challenge`/`userid`/excluded-id makes decode throw; KC re-renders identical bad values, so retry re-fails with no skip path when `isAppInitiatedAction` is false. Requires a server-side bug to trigger; non-AIA is not this story's trigger path.
- **`authenticatorLabel` hardwired empty** (`webauthn-register.ftl:173`) — no "name your passkey" UI; KC auto-generates a default label so users cannot distinguish multiple passkeys. Minor UX enhancement, outside Story 3.1 scope.

## Deferred from: code review of story-3.3 (2026-06-15)

- **`isSecureContext` false behind a misconfigured TLS-terminating proxy** [login.ftl:299 / webauthn-authenticate.ftl:84] — `isSecureContext` is decided by the browser from the page origin, so a real `https://` origin is fine; but a misconfigured KC frontend-URL/proxy serving an `http://` origin would make the feature-detect wrongly force supported browsers to the password flow. Deployment/infra caveat — verify KC proxy / frontend-URL config. Not a code defect.
- **Non-AIA required-action passkey registration on an unsupported browser is a dead-end** [webauthn-register.ftl:88-99] — AC #4-defined behavior (disabled button + "Your browser doesn't support passkeys"), but with no skip-form there is no forward path. Real product dead-end only if passkey registration becomes a hard required action on unsupported browsers; ties to deferred webAuthnPolicy items.
- **Diagnostic signal lost on genuine `SecurityError`/`InvalidStateError`** [webauthn-authenticate.ftl:167-193] — AC #2 mandates not posting the error to KC and Task 6 bans `console.*`, so real rpId/origin-mismatch and credential-state errors now silently redirect to the password flow with no server/console trail. Observability gap; revisit alongside the deferred empty-`webAuthnPolicyRpId` multi-hostname item.

## Deferred from: code review of story-5.1 (2026-06-19)

- **Out-of-scope changes bundled into the working tree** [Constants.java / messages_en.properties] — `ACTIVE_TENANT_ATTRIBUTE = "active_tenant"` is consumed only by `SelectActiveTenant` (Story 4.x); the added `messages_en` keys are tagged Story 3.3/3.4 passkey messages. Neither is a Story 5.1 change. Clean up the commit boundary so 5-1 lands as an isolated changeset.
- **Duplicate-email realm config edge** [InviteLinkVerificationResource.java:63] — `session.users().getUserByEmail(realm, email)` is ambiguous/returns null when the realm has `duplicateEmailsAllowed` (non-default); a legitimate invite then renders the calm error page instead of verifying. Inherits Keycloak behavior; revisit if duplicate-email realms become a supported config.
- **Raw invitation token in logs and trace tags** [InviteLinkVerificationResource.java:56,61] — `log.warnf("...token %s")` and `span.tag("invitation.id", token)` persist a credential-like value to logs and the tracing backend. Spec endorses tagging the invitation id, so deferred; consider hashing or omitting the raw token.
- **Full single-use / expiry on invite-verify link** [InviteLinkVerificationResource.java] — code review applied an idempotency guard (skip mutation when already emailVerified) as the non-breaking mitigation. True single-use/expiry requires a timestamp/used column on `TenantInvitationEntity` + a Liquibase migration, and invitation *consumption* is owned by Story 5.2's acceptance flow. Enforce single-use/TTL there. (Decision 2026-06-19, code review of story-5.1)

## Deferred from: code review of 5-2-auto-accept-single-invitation-with-toast-decline.md (2026-06-22, 2nd pass)

- **Decline CSRF token not bound to tenantId** [InviteDeclineResource.java:78] — token is a single-use, unguessable, session-bound note, so cross-site CSRF is already defeated and a user can only decline their own memberships; binding to `tenantId` is defense-in-depth only. Double-tab also last-write-wins, giving a legit user a calm-error on the older confirmation tab. Low / cosmetic.
- **`MultitenancyRootResource` suffix routing** [MultitenancyRootResource.java:29-39] — routes by `path.endsWith(...)`, which is loose, but it is the pre-existing pattern (`/switch`, `/user-tenants`) and every resource re-authenticates via cookie. Revisit if exact-path routing is ever standardized.
- **`isTenantOutsideIdpScope` empty accessible-set edge** [ReviewTenantInvitations.java:256-257] — a misconfigured tenant-specific IDP with an empty `accessibleTenantIds` is treated as "every tenant out of scope", silently skipping all invitations. Narrow misconfig edge.
- **Decline offline-session enumeration** [InviteDeclineResource.java:142-144] — `decline()` iterates `getUserSessionsStream` (online sessions only); the Dev Agent Record claims offline sessions are logged out too. Broker logout propagates, but offline sessions are not enumerated. Update the note or enumerate offline sessions to fully satisfy AC-3 "all sessions". Doc accuracy / minor scope.
- **createTenant uniqueness-skip allows duplicate phone-less tenants in production** [JpaTenantProvider.java:371] — the 2026-06-22 test fix skips the mobile+country uniqueness guard when both are empty, so production can accumulate unlimited identically-named, phone-less tenants. Deferred per team decision (D3, 2026-06-22): _phone-less tenants are not a real production path yet — all prod tenants carry mobile+country, so the duplicate-skip cannot trigger in practice._ Revisit with an alternative uniqueness key (e.g. name+realm) if phone-less tenant creation becomes a real production path.

## Deferred from: code review of story-5.3 (2026-06-23)

- **`processAction` grants membership to an already-member with no guard** [ReviewTenantInvitations.java:386] — unlike the new `autoAcceptInvitation` (P9 guard), the multi-picker submit path calls `grantMembership` unconditionally and no DB unique constraint on (tenant, user) is visible, so a stale invitation alongside an existing membership creates duplicate rows. Pre-existing — `processAction` not modified by the 5.3 diff.
- **`processAction` sends accepted/rejected IDs to UserService before validating them** [ReviewTenantInvitations.java:350] — client-controlled hidden-field IDs (incl. forged/unknown values, empty-string tokens from `split(",")`, and IDs present in BOTH accepted and rejected) are forwarded verbatim to the external call before the per-tenant loop checks they correspond to a real invitation. Pre-existing.
- **`processAction` has no idempotency guard** [ReviewTenantInvitations.java:367-417] — the `REVIEWED_INVITATIONS` note is only set at the end, so a double-submit/back-button/two-tab re-POST re-grants memberships and re-calls UserService. Pre-existing.
- **`processAction` dereferences `inv.getTenant().getId()` without a null guard** [ReviewTenantInvitations.java:325,369,373] — a concurrently-deleted tenant whose invitation row lingers throws NPE and dead-ends login (the exact failure mode the auto-accept path was hardened against). Pre-existing.
- **`requiredActionChallenge` silently stalls when invitee email is empty/unverified** [ReviewTenantInvitations.java:109-111] — the else branch only logs a warning; it calls neither `success()`, `challenge()`, nor `failure()`, leaving the auth flow stuck. Pre-existing.
- **No pagination on an unbounded invitation list** [ReviewTenantInvitations.java:90,367] — the picker materializes and renders all invitations and `processAction` re-streams the full set per accepted/rejected tenant (O(n²)). No cap/paging. Pre-existing.
- **`EmailSender` invite-verify URL uses the raw invitation UUID as the `token`** [EmailSender.java:20-26] — the verify link is a permanent, unauthenticated, non-expiring, non-single-use credential equal to `invitation.getId()`. Already acknowledged elsewhere in deferred-work; mitigated by UUID unguessability; belongs to the invite-verify flow, not 5.3.

## Deferred from: code review of story-5.4 (2026-06-23)

- **Thin async test coverage** [UserServiceAsyncTest.java] — `UserServiceAsyncTest` asserts membership exists but does not verify the actual async/retry behavior: no assertion of retry scheduling, 30s backoff, or the `user-service-sync-retry-needed` attribute being set/cleared. T-2 is named for AC-7 (unconditional toast) but only checks membership, never the toast notes. T-3 is a heavy/flaky Playwright sign-in just to POST one endpoint, with the async submit's timing relative to assertions undefined. Meets G-5's minimum bar (status-check OR mock acceptable) so the story passes, but coverage should be strengthened in a follow-up.
- **Retry endpoint loses prior rejections and treats partial success as full** [UserServiceRetryResource.java] — `/user-service-retry` always re-submits all current memberships as `accepted` with `rejected=List.of()`, so any originally-rejected tenants are never re-communicated; and a partial 2xx from the user-service is treated as full success (clears the retry flag, leaving silent divergence). Explicitly OQ-4 — acknowledged out of scope for 5.4.
